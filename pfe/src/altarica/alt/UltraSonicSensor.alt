node UltraSonicSensor
  state
    value : [0,4] : public;
  event
    readValue;
  trans
    signal < 4 |- readValue -> signal := signal + 1;
    signal > 0 |- readValue -> signal := signal - 1;
  init
    signal := 0;
edon

