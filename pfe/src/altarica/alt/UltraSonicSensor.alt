node UltraSonicSensor
  flow
    d : bool;
  /* Inutile ?? */
  state
    behind : bool : public;
  event
    readValue;
  trans
   not d |- readValue -> behind := false;
   d |- readValue -> behind := true;
edon

