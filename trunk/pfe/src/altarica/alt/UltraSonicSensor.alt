node UltraSonicSensor
  state
    signal : [0,4] : public;
  event
    inc, dec;
  trans
    signal < 4 |- inc -> signal := signal + 1;
    signal > 0 |- dec -> signal := signal - 1;
  init
    signal := 0;
edon

