node UltraSonicSensor
  state
    value : [0,2] : public;
  event
    readValue;
  trans
    signal < 2 |- readValue -> signal := signal + 1;
    signal > 0 |- readValue -> signal := signal - 1;
  init
    signal := 0;
edon

